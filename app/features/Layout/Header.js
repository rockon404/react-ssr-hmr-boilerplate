import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  padding: 24px;
  color: #61dafb;
  background-color: #20232a;
`;

const Header = () => <Wrapper>React SSR HMR Boilerplate</Wrapper>;

export default Header;
